{
    "compilerOptions": {
      "module": "Node16",
      "moduleResolution": "node16",
      "types": ["@sveltejs/kit"],
      // other options...
    },
    "exclude": [
      "node_modules",  // Always exclude this folder
      "build",         // Exclude build folder if present
      "dist",          // Exclude dist folder if present
      // Add any other directories you want to exclude
    ],
    "extends": "./.svelte-kit/tsconfig.json"
  }
  